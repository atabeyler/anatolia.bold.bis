# Anatolia B.I.S. — Client

React + TypeScript + Vite frontend. See the repository root `README.md` for
project context and `docs/ARCHITECTURE.md` for the overall system design.

```bash
npm install
npm run dev        # local dev server
npm run typecheck
npm run test
npm run build
```
